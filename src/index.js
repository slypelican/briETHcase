import React from 'react';
import ReactDOM, { createRoot } from 'react-dom/client';
// import './styles.css';

import App from '../mockup/App.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />
);